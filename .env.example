PORT=8787
SVN_DATA_DIR=./server/data

# Add the selected payment provider's credentials and webhook secret here.
# Membership activation must call finalizeVerifiedPayment only after authentic
# server-side webhook verification. Do not expose secrets to browser code.
PAYMENT_PROVIDER=
PAYMENT_WEBHOOK_SECRET=
